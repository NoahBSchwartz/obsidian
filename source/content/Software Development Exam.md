- Must know these commands: pwd , ls , ls –l , cd (home) , cd (down) , mkdir, rmdir, rm, touch, cp (from) (to), mv (from) (to), echo “this” >> target, cat, vim
- Don't memorize metacharacters but know how to build regular expressions given a table of metacharacters
- What is the key characteristic of a statically-typed language? Variable types are checked at compile-time
- Use backticks for string concatenation:
```js
res.send(`Here is the recipe for ${id}`)
```

